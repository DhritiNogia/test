Few points to run the code locally:

1) Please install the node modules locally using "npm install"
2) To run the app locally, start the backend server using uvicorn and then change the api endpoint at api.js in the Frontend folder.

